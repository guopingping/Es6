读懂ECMAScript风格

```
1、术语
    抽象操作
        abstractor oprations
        引擎的一些内部方法，外部不能调用；
    Record和field
        key-value map的数据结构称为Record
        每一组键值对为field
    [[Notation]]
        用来指导field的键名；
        对象的内部属性
    Completion Record
        运行结果
        运行结果的类型：[[Type]]    
            normal/return/throw/break/continue
2、抽象操作的标准流程

3、相等运算符
    ==

4、数组的空位

5、数组的map方法
    跳过空位
    
```